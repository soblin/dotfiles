;;; 01_which_key.el --- <Summary> -*- lexical-binding: t; -*-

;;; Commentary:

;;; Code:


(use-package which-key
  :ensure t
  :config
  (which-key-mode 1)
  )

(provide '01_which_key)
;;; 01_which_key.el ends here

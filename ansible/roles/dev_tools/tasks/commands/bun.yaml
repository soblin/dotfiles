# bun (provides bunx)
- name: setup bun
  become: false
  block:
    - name: install bun
      ansible.builtin.shell: >-
        curl -fsSL https://bun.com/install | bash
      args:
        creates: "{{ lookup('env','HOME') }}/.bun/bin/bun"

- name: install system commands
  apt:
    name:
      - etckeeper
      - git
      - git-lfs
      - curl
      - tree
      - xclip
      - xsel
      - mlocate
      - htop
      - direnv
    state: present # NOTE: if not installed, install

- import_tasks: commands/tmux.yaml
- import_tasks: commands/alacritty.yaml
